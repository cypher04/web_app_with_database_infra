# For local development — set these values and rename to .env
# On Azure App Service, DATABASE_URL is set automatically via app_settings

DATABASE_URL=Server=localhost;Database=maindb;User Id=sa;Password=yourpassword;

# Or use individual vars:
# DB_SERVER=localhost
# DB_NAME=maindb
# DB_USER=sa
# DB_PASSWORD=yourpassword

PORT=3000
NODE_ENV=development
